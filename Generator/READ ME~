+ Generate the sql scripts : 
	tab OWL to SQL
		first field : path of the ontology
		second field : path of the generated scripts
		press generate

+ Generate the C++ classes :
	tab OWL to C++
		first field : path of the ontology
		second field : path of the generated classes
		URl : url of your data base
		User name and password : login of your mysql account
		schema : data base name
