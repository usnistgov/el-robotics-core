int kitNumber=1;
int planNumber=1;
String filePath = "/home/zeid/Documents/Assembla/zeid/workspace/IntentionRecognition/etc/kits/Kit"+kitNumber+planNumber+"state.txt";
Scanner scanner = new Scanner(new File(filePath);
while (scanner.hasNextLine()) {
	String line = scanner.nextLine();
	ArrayList<String> states=new ArrayList<String>(Arrays.asList(line.split(",")));
}
scanner.close();
	
