To build code:
1) Goto the database directory
  a) Edit the Connection in the file DAO.cpp to contain the host,
    user,  password, and database for your installation.
  b) run make in this directory
2)  In the source directory, run make

